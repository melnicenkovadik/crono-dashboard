import type { Workspace } from './types'

export const workspace: Workspace = {
  greetingName: 'Alex',
  periodLabel: 'May',
  inboxCount: 24,
  trial: { daysLeft: 2 },
  profile: { name: 'William Robertson', role: 'Sales' },
}
